# ⚽ Ronaldo_Project  <SoftWare>

```
FLOW CHART
    subgraph "1. 시작"
        A(🎮 게임 시작) --> B[타이틀 화면];
        B --> C{🕹️ 1인용 / 2인용 선택};
    end

    subgraph "2. 준비"
        C --> D[📸 얼굴 등록 화면];
        D --> E{네모 칸에 얼굴 맞추기};
        E -- 찰칵! --> F[얼굴 사진 촬영 완료!];
    end

    subgraph "3. 게임 플레이 (5회 반복)"
        F --> G([ዙ 라운드 시작]);
        G --> H[⏳ 5초 카운트다운];
        H --> I{🥅 막을 곳 / 찰 곳 선택};
        I --> J[⚽ 공이 날아감!];
        J --> K{결과 판정};
    end

    subgraph "4. 결과 확인"
        K -- 성공! --> L[🎉 성공 GIF 재생];
        K -- 실패! --> M[😭 실패 GIF 재생];
        L --> N{기회가 남았나?};
        M --> N;
    end
    
    N -- 예 --> G;

    subgraph "5. 최종 결과"
        N -- 아니오 --> O[🏆 최종 결과 화면];
        O --> P[점수, 랭크와 함께<br>내 얼굴 사진 표시];
        P --> Q{다시 플레이?};
    end

    subgraph "6. 마무리"
        Q -- 다시 하기 --> C;
        Q -- 그만하기 --> R(👋 게임 종료);
    end
```